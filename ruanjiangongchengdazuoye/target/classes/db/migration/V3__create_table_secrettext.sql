CREATE TABLE SECRETTEXT
(
	ID INT NOT NULL,
	USERID INT,
	TITLE TEXT,
	ARTICLE TEXT,
	TIME VARCHAR(30),
	INFO TEXT,
	PRIMARY KEY(ID),
	FOREIGN KEY (USERID) REFERENCES MANAGER(ID)
) ENGINE=InnoDB DEFAULT CHARSET=utf8;
